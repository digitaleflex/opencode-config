interface:
  display_name: "Opencode Config"
  short_description: "Repo-specific patterns and workflows for opencode-config"
  default_prompt: "Use the opencode-config repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true